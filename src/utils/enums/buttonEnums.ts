export enum EButtonClass {
  DEF = 'def',
  SEC = 'sec',
}

export enum EButtonSize {
  LG = 'lg',
  SM = 'sm',
}

export enum EButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}
