enum EActionType {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}

enum EFavoriteActionType {
  ADD = 'add',
  REMOVE = 'remove',
  DELETE_ALL = 'delete_all',
}

export { EActionType, EFavoriteActionType };
